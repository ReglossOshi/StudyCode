create sequence seq_board;

create table tbl_board(
	bno number(10,0) CONSTRAINT pk_board PRIMARY key,
	title varchar2(200) not null,
	content varchar2(2000) not null,
	writer varchar2(50) not null,
	regdate date default sysdate,
	updatedate date default sysdate
);

alter table tbl_board add constraint pk_board primary key (bno);

ALTER TABLE TBL_BOARD  ADD updatedate date default sysdate;

insert into tbl_board (bno, title, content, writer) 
values (seq_board.nextval, '테스트 제목','테스트 내용','user00');

SELECT seq_board.nextval FROM DUAL ;

SELECT BNO,TITLE,CONTENT 
FROM
(SELECT /*+ INDEX_ASC(TBL_BOARD PK_BOARD)*/ROWNUM RN, BNO, TITLE, CONTENT 
FROM TBL_BOARD 
WHERE ROWNUM<=20)
WHERE RN > 10;

SELECT /*+ INDEX_ASC(TBL_BOARD PK_BOARD)*/ROWNUM RN, BNO, TITLE, CONTENT 
FROM TBL_BOARD 
WHERE ROWNUM<=20;


select * from tbl_board where ();

SELECT * FROM BO_FREE bf;


CREATE TABLE TBL_REPLY(
	rno number(10,0),
	bno number(10,0) NOT null,
	reply varchar2(1000) NOT null,
	replyer varchar2(50) NOT null,
	replyDate DATE DEFAULT sysdate,
	updateDate DATE DEFAULT sysdate
);

CREATE SEQUENCE SEQ_REPLY;
ALTER TABLE TBL_REPLY ADD CONSTRAINT PK_REPLY PRIMARY KEY(rno);
ALTER TABLE TBL_REPLY ADD CONSTRAINT FK_REPLY_BOARD FOREIGN KEY(bno) REFERENCES tbl_board(bno);


SELECT num, subject, TO_CHAR(regdate,'YYYY-MM-DD'),hit 
		FROM BO_FREE 
		WHERE subject LIKE '%'||?||'%' 

		
		SELECT NUM, NAME, SUBJECT, CONTENT, substr(FILE1_PATH,1,4), FILE2_PATH 
    	FROM BO_FREE WHERE NUM = 49;
    	
    UPDATE BO_FREE SET NAME = 홍삼캔디, SUBJECT = 미아러, CONTENT = 미아ㅓㄹ                         WHERE NUM = 49;
    
   
SELECT bno FROM TBL_BOARD tb;